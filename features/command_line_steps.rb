
Given /^a running tmux session with a window title of "(.*?)"$/ do |title|
  #`tmux new-session #{title}`
  pending
end

Then /^a new real tmux session should be started$/ do
  pending
end

Then /^the real tmux session window title should be "(.*?)"$/ do |title|
  pending
end
